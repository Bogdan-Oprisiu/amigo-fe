import React from 'react';
import NavBar from "../nav_bar/NavigationBar";

const Settings = () => {
    return (
        <div>
            <NavBar/>
            <h1>Settings</h1>
        </div>
    );
};

export default Settings;