import React from 'react';
import NavBar from "../nav_bar/NavigationBar";

const Profile = () => {
    return (
        <div>
            <NavBar />
            <h1>Profile Page</h1>
        </div>
    );
};

export default Profile;